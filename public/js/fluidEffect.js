// Original event listener for drag
// element.addEventListener('drag', handleFluidEffect);

// Change to hover event
element.addEventListener('mouseenter', handleFluidEffect);

function handleFluidEffect(event) {
    // Logic to create the fluid effect
    // This function should be adapted to work on hover
} 